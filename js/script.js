window.onload = function() {
  
  function buildBoard() {
    var board = document.getElementById('board');
    
    for (var i = 0; i < 9; i++) {
      // build a row
      var row = document.createElement('div')
      row.style.classList
    }
  }
};
